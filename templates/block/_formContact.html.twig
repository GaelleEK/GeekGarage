
{% block contact %}
    <section id="contact">
        <div class="container">
            <div class="form-group" id="formContact">
                {{ form_start(form) }}
{#                  Exemple pour afficher selon la route du rendu                         #}
{#                {% set route = app.request.attributes.get('_route') %}#}
{#                {% if route  == 'home_contact' %}#}
                    <h3 class="text-center p-5 text-uppercase">Contacter le centre de {{ center.city }}</h3>
{#                {% else %}#}
{#                    <h3 class="text-center p-5 text-uppercase text-white">Contactez-nous</h3>#}
{#                {% endif %}#}


                    {{ form_row(form.first_name) }}
                    {{ form_row(form.name) }}
                    {{ form_row(form.tel) }}
                    {{ form_row(form.mail) }}
                    {{ form_row(form.message) }}
                    <div class="d-flex justify-content-end">{{ form_row(form.submit) }}</div>

                {{ form_widget(form._token) }}
{#                {% if route == 'home_contact' %}#}
{#                    {{ form_end(form, {'render_rest': false}) }}#}
{#                {% else %}#}
                    {{ form_end(form) }}
{#                {% endif %}#}
            </div>
        </div>
    </section>
{% endblock %}